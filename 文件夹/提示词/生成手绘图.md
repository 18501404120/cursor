根据文档内容提炼结构，生成手绘图，并本地化保存为「新建文件夹 + 3 个文件」。

---

## 执行前必做：自动启动 Excalidraw 画布服务

生成手绘图前**必须先确保 Excalidraw 画布服务已运行**（否则 @excalidraw 会报连接失败）。请按以下任一方式启动：

1. **推荐**：在终端执行脚本（若 3000 端口已有服务会自动跳过）
  `bash "/Users/ckw/Desktop/cursor文件夹/方案设计/文件夹/脚本/启动Excalidraw画布服务.sh"`
2. **或** 手动启动：
  `cd /Users/ckw/.cursor/mcp_excalidraw-main && node dist/server.js`  
   需在后台运行并等待约 2 秒，确认 [http://localhost:3000](http://localhost:3000) 可访问后，再调用 @excalidraw 进行绘图。

---

## 可直接复制的提示词

```text

生成手绘图前先自动启动 Excalidraw 画布服务：执行 bash "/Users/ckw/Desktop/cursor文件夹/方案设计/文件夹/脚本/启动Excalidraw画布服务.sh" ，确认服务就绪后再进行下图步骤。

根据上述提供的内容
通过 @excalidraw 生成一张手绘风格的图文总结。要求：内容详细，不可遗漏要点、逻辑清晰、布局合理，配色/字体/组件风格统一，符合视觉规范；在图中用分区清晰呈现上述结构。

风格参考：/Users/ckw/Desktop/cursor文件夹/方案设计/文件夹/参考图/钉钉听记.png



同时把结果本地化保存：
1. 在 /Users/ckw/Desktop/cursor文件夹/方案设计/文件夹/手绘图 下新建文件夹，文件夹命名为「文档总结后的标题+时间yyddmmhhmm」；
2. 在上述新建文件夹下生成 3 个文件（HTML 预览入口 + PNG + .excalidraw 源文件），文件名使用「文档总结后的标题+时间yyddmmhhmm」，即：
   - <标题>-打开预览.html
   - <标题>.png
   - <标题>.excalidraw
```

---

---

## 输出说明


| 项目    | 说明                                                                        |
| ----- | ------------------------------------------------------------------------- |
| 新建文件夹 | 路径：`/Users/ckw/Desktop/cursor文件夹/方案设计/文件夹/手绘图/<文档总结后的标题+时间`yyddmmhhmm`>/` |
| HTML  | `<标题>-打开预览.html`，页内须嵌入完整结论/规则/待办等文字，不依赖 PNG 即可阅读                          |
| PNG   | `<标题>.png`，手绘图预览（需在 Excalidraw Canvas 中导出）                                |
| 源文件   | `<标题>.excalidraw`，可二次编辑                                                   |


