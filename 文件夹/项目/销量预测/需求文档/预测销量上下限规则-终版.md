# 预测销量上下限与系统推荐（终版）

因子与验收细则见：[预测销量上下限规则-PRD.md](./预测销量上下限规则-PRD.md) 第 4～6 章。  
本文：**上半部分 = 当前生效逻辑（与简化版一致）**；**下半部分 = 按步骤给出的数据示例**（预测目标月 **2026-05**，汇总范围 **MSKU A / B / C**）。

---

## 一、当前规则（逻辑口径）

### 在算什么

每个货号、每个月先有**客观下限销量、客观中值销量、客观上限销量**（都不读销售目标）。再用各货号**近30天平均价**把三档销量加成**全组悲观营收、全组中间营收、全组乐观营收**。最后用**目标总营收**决定每个货号的**推荐销量**。

### 怎么算（按顺序）

1. **客观中值销量**：规则/模型，只看客观因子，不看目标。  
2. **客观下限、上限销量**：与中值同一套销量公式，因子取悲观/乐观一侧（流量、转化、竞争力、广告、价格等按 PRD 第 4 章取历史极值或缩放）。  
3. **三档全组营收**：各货号悲观/中间/乐观销量 × 该货号**近30天平均价**，再在汇总范围加总，得全组悲观、中间、乐观营收。  
4. **推荐销量（看目标总营收）**  
   - 无有效目标 → 推荐 = **客观中值销量**。  
   - 目标 **高于** 全组乐观营收 → 推荐 = **客观上限销量**。  
   - 目标 **低于** 全组悲观营收 → 推荐 = **客观中值销量**。  
   - 目标 **不低于悲观且不高于乐观** → 倍数 = 目标 ÷ 全组中间营收，推荐 = **客观中值销量 × 倍数**（再取整）。  
5. **落地**：推荐销量取整；**不得高于该货号客观上限、不得低于该货号客观下限**；必要时在边界内微调几件货使汇总可接受并留痕。

### 汇总范围

默认：**当前专员权限内负责的全部货号**。若用户筛选子集，只算子集且界面须标明。

### 推荐销量（`R_i`）计算逻辑（与步骤 4 对应）

在已得到 **`Lo_i`、`Mid_i`、`Hi_i`** 与 **`RevLo`、`RevMid`、`RevHi`**（均由**近30天平均价**折算）之后，对每个货号 `i`：

1. **无有效目标总营收 `T_rev`**：`R_i = Mid_i`。  
2. **`T_rev > RevHi`（目标高于全组乐观营收）**：`R_i = Hi_i`。  
3. **`T_rev < RevLo`（目标低于全组悲观营收）**：`R_i = Mid_i`。  
4. **`RevLo ≤ T_rev ≤ RevHi`**：先算汇总倍数 **`m = T_rev ÷ RevMid`**，再 **`R_i = ROUND(Mid_i × m)`**，然后 **`R_i` 不得小于 `Lo_i`、不得大于 `Hi_i`**（超出则先卡到边界，再按落地规则在允许范围内做二次微调，使全组营收与目标误差落在阈值内）。

**口径提醒**：`Rev*` 一律用各货号 **近30天平均价 × 对应档位销量** 后加总；**不要**用「进销量公式里的月均价极值」去折 `Rev*`，二者职责分离（见第二节）。

---

## 二、因子口径摘要（对齐 PRD 第 4 章）

在与客观中值**相同的销量合成公式**里，只替换下面五类因子；悲观取**不利销量**一侧，乐观取**有利销量**一侧；其余与中值一致。

| 因子 | 客观下限（悲观） | 客观上限（乐观） | 窗口与聚合 |
|------|------------------|------------------|------------|
| **流量** | 预测流量 × **0.5** | 预测流量 × **2** | 与预测月、同粒度对齐 |
| **转化率** | 近 3 个月各月代表值的 **min** | 近 3 个月各月代表值的 **max** | **不含**目标月 |
| **竞争力** | 近 3 个月代表值的 **min** | 近 3 个月代表值的 **max** | **不含**目标月；本例代表值为**月同比增长率**（如 **-2.5%**、**+14%**） |
| **价格**（进销量公式） | 近 1 年各月**月均价**的 **max**（价高偏悲观） | 近 1 年各月**月均价**的 **min**（价低偏乐观） | **不含**目标月 |
| **广告花费** | 近 3 个月月代表值的 **min** | 近 3 个月月代表值的 **max** | 与中值路径币种/口径一致 |

**与营收折算价的区别**：上表中的「价格」参与**销量**合成；把算好的 `Lo/Mid/Hi` 销量折成**营收**时，使用下文「近30天平均价」，二者**不得混用口径**（见 PRD 第 2 章与第 4 章边界说明）。

---

## 三、数据示例（按步骤）

**约定**：便于手算复现，本例采用与 PRD **第 6.1 节**相同的**示意销量公式**（生产以规则/ML 同构公式为准）：

`Q = ROUND(流量路径 × 转化率路径 × (1 + g) × 5)`  

其中 `g` 为竞争力**月同比增长率**的小数形式（如 **+2% → 0.02**，**-2.5% → -0.025**）。  
**说明**：下表已按 PRD **第 4 章**把**价格（进销量公式的月均价）**与**近30天平均价（仅折营收）**一并列出，满足「五类因子 + 折算价」同屏对照；本算例的**件数三线**仍与 PRD 6.1 **数值对齐**（该节 toy 式未显式乘「价因子」；产线若以完整同构式含价，需用表中价格代入重算并替换步骤 2 的件数）。

---

### 步骤 1：目标月（2026-05）中值路径因子 + 流量在悲观/乐观路径的替换

**中值路径（预测目标月当月）** — 与截图结构一致，并补充**价格**与**近30天均价**两列：

| MSKU | 预测流量 | 转化率（中值路径代表值） | 竞争力：近 3 个月同比增长率（中值路径代表值） | 广告花费（代表值，示意） | **价格（进销量公式：月均价中值代表，元/件）** | **近30天平均价（仅折营收，元/件）** |
|------|----------|--------------------------|-----------------------------------------------|---------------------------|-----------------------------------------------|----------------------------------------|
| A | 400 | 9.0% | +2.0% | 230 | **10.5** | **12.0** |
| B | 600 | 12.0% | +3.5% | 300 | **13.8** | **15.0** |
| C | 200 | 5.5% | +0.8% | 100 | **18.0** | **20.0** |

**悲观/乐观路径下的预测流量**（仅流量因子按 PRD 缩放，其余因子在历史窗内取 min/max 后代入同式，此处不展开分月表）

| MSKU | 客观下限路径流量（×0.5） | 客观上限路径流量（×2） |
|------|---------------------------|------------------------|
| A | 200 | 800 |
| B | 300 | 1,200 |
| C | 100 | 400 |

**历史极值用于悲观/乐观路径的示意（不含 2026-05）** — 与 PRD **第 4 章**一致：转化率、竞争力、广告取近 3 月 **min / max**；**价格（月均价）**取近 1 年 **max（悲观）/ min（乐观）**。

| MSKU | 转化率：悲观 min → 乐观 max | 竞争力 g：悲观 min → 乐观 max | 广告：悲观 min → 乐观 max | **价格（月均价）：悲观 max → 乐观 min（元/件）** |
|------|-----------------------------|--------------------------------|---------------------------|--------------------------------------------------|
| A | 8.0% → 10.0% | **-2.5%** → **+14%** | 200 → 260 | **12.0** → **9.0** |
| B | 11.0% → 13.0% | +2.0% → **+6.4%** | 280 → 320 | **15.5** → **12.5** |
| C | 5.0% → 6.0% | **-2.0%** → +1.0% | 90 → 110 | **20.0** → **16.5** |

（上表为「五类因子」在悲观/乐观路径上的取值来源示意；本例步骤 2 的 `Lo/Mid/Hi` **件数**仍直接采用 PRD 6.1 已算结果，便于与主文档对账；若实现侧 toy/规则式显式含「价因子」，应以上表与中值路径价格代入后重算件数。）

---

### 步骤 2：客观销量三线（件）

| MSKU | 客观中值 `Mid_i` | 客观下限 `Lo_i` | 客观上限 `Hi_i` |
|------|------------------|-----------------|-----------------|
| A | **184** | **79** | **413** |
| B | **373** | **168** | **830** |
| C | **55** | **25** | **121** |

件数汇总：`SumLo = 272`，`SumMid = 612`，`SumHi = 1,364`。

---

### 步骤 3：近30天平均价 → 三档全组营收（元）

**近30天平均价**（元/件，**仅用于营收折算**）：A **12.0**，B **15.0**，C **20.0**。

| 汇总量 | 计算式 | 结果（元） |
|--------|--------|------------|
| **全组中间营收 `RevMid`** | `184×12 + 373×15 + 55×20` | **8,903** |
| **全组悲观营收 `RevLo`** | `79×12 + 168×15 + 25×20` | **3,968** |
| **全组乐观营收 `RevHi`** | `413×12 + 830×15 + 121×20` | **19,826** |

---

### 步骤 4：目标总营收 `T_rev` 三种情况 → 推荐销量 `R_i`（件）

以下均在同上 **A/B/C** 汇总范围内。具体判定与公式见 **第一节「推荐销量（`R_i`）计算逻辑」**。

**无目标**：`R_i = Mid_i` → **184 / 373 / 55**。

#### 情况甲：目标 **高于** 全组乐观营收（`T_rev > RevHi`）

- 设 **`T_rev = 25,000` 元**（`25,000 > 19,826 = RevHi`）。  
- **逻辑**：目标超出乐观营收上沿，推荐**顶到乐观销量档**，不再按比例外推（避免暗示可超过客观上界）。  
- **计算**：对每个 `i`，**`R_i = Hi_i`**。

| MSKU | `R_i`（= `Hi_i`） |
|------|-------------------|
| A | **413** |
| B | **830** |
| C | **121** |

（此档下用**近30天平均价**折算的全组营收为 **`Σ(Hi_i × 近30天均价_i)` = 19,826** 元，等于 `RevHi`。）

#### 情况乙：目标 **低于** 全组悲观营收（`T_rev < RevLo`）

- 设 **`T_rev = 3,000` 元**（`3,000 < 3,968 = RevLo`）。  
- **逻辑**：目标低于悲观营收下沿，**不按目标把推荐销量往下压**，避免跟「执行上不可达」的过低目标跑飞。  
- **计算**：对每个 `i`，**`R_i = Mid_i`**。

| MSKU | `R_i`（= `Mid_i`） |
|------|--------------------|
| A | **184** |
| B | **373** |
| C | **55** |

#### 情况丙：目标 **介于** 悲观与乐观之间（`RevLo ≤ T_rev ≤ RevHi`）

- 设 **`T_rev = 12,000` 元**（`3,968 ≤ 12,000 ≤ 19,826`）。  
- **逻辑**：目标落在「中值营收」可解释的带内，用**单一汇总倍数**把各货号中值销量同比例抬起或压低，再卡到各货号客观上下限内。  
- **计算**：  
  1. **`m = T_rev ÷ RevMid`** → `12,000 ÷ 8,903 ≈ 1.347860…`  
  2. **`R_i = ROUND(Mid_i × m)`**  
  3. **`R_i = min(max(R_i, Lo_i), Hi_i)`**（本例未触发越界）

| MSKU | 计算 | `R_i` |
|------|------|--------|
| A | `ROUND(184 × 1.347860…)` | **248** |
| B | `ROUND(373 × 1.347860…)` | **503** |
| C | `ROUND(55 × 1.347860…)` | **74** |

验算全组营收（仍用步骤 3 的**近30天平均价**）：`248×12 + 503×15 + 74×20 = **12,001**` 元，与目标 **12,000** 相差 **1** 元，属取整误差；落地规则可按 PRD **二次缩放**在 `Lo_i～Hi_i` 内消化至阈值内。

---

### 步骤 5：落地与展示（与示例对齐）

| MSKU | 客观下限 | 情况甲推荐 | 情况乙推荐 | 情况丙推荐 | 客观上限 |
|------|----------|------------|------------|------------|----------|
| A | 79 | 413 | 184 | 248 | 413 |
| B | 168 | 830 | 373 | 503 | 830 |
| C | 25 | 121 | 55 | 74 | 121 |

---

## 四、版本说明

| 项 | 内容 |
|----|------|
| **规则正文** | 与 [预测销量上下限规则-简化版.md](./预测销量上下限规则-简化版.md) 当前版本一致。 |
| **因子与三线件数** | 与 PRD **第 4 章、第 6.1 节**算例对齐；步骤 1 表含**进销量月均价**与**近30天均价（折营收）**；**营收折算价**在本文中明确为 **近30天平均价**（PRD 正文若仍写「近一年基准价」，以实现/业务统一口径后的版本为准）。 |
