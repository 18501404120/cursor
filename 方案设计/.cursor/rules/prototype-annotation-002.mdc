---
description: 用户说「原型标注002」「原型标注 002」「通过原型标注002对当前原型页面进行标注」「用原型标注002」「原型标注2」或局域网分享标注时，必须读取并严格按 `.cursor/skills/prototype-annotation-002/SKILL.md` 执行。GitHub Pages 仍用 001。
alwaysApply: false
---

# 原型标注002（局域网分享 · phrase 触发）

## 必须执行

1. **先读取** Skill 全文：`.cursor/skills/prototype-annotation-002/SKILL.md`，并读取 `references/ai-persistence-policy.md` 与 `references/edit-policy-setup.md`。
2. **目标页面**：优先当前聚焦的 HTML；不明确时只问一次路径。
3. **与 001 区分**：GitHub Pages → 001；原型分享服务（8787）→ **002**。
4. 注入块 id 为 `prototype-annotation-002-config`；标记 `prototype-annotation-002:start/end`。

## 两种模式

| 用户意图 | Agent |
| --- | --- |
| 改页面 / 改需求（未要求刷新标注） | 禁止动 `#prototype-annotation-002-config`、`persistedState`、002 runtime 块 |
| 「刷新/更新 原型标注002」 | `--preserve-persisted`（默认） |
| 「全量重生成 / 不要保留标注修改」 | `--no-preserve-persisted` |

## 资源路径

`.cursor/skills/prototype-annotation-002/` 下 `assets/`、`scripts/`、`references/`。

## 不要做的事

- 对局域网分享页面注入 **001** runtime（含 GitHub Pages IP 策略）。
- 在分享链接上去掉 `paView=1`（分享服务负责附加，Agent 勿改分享服务逻辑除非用户要求）。
