---
description: 场景梳理001 / 框架梳理001：从会议或背景输入生成 方案/*-场景梳理.html 叙事页（PRD 之前）。触发时读取 .cursor/skills/scenario-framing-001/SKILL.md。
globs:
alwaysApply: false
---

# 场景梳理 001

当用户说 **场景梳理001**、**框架梳理001**、**场景梳理001skill**、**框架梳理001skill** 或同义（场景梳理页、框架梳理、需求对齐 HTML）时：

1. **立即读取** `.cursor/skills/scenario-framing-001/SKILL.md` 并严格按流程执行。
2. 产出路径：`文件夹/需求/{名}/方案/` 或 `文件夹/项目/{名}/方案/`（无则创建）；文件名 `{主题}-场景梳理.html`，再版 `-v2`、`-v3`。
3. **禁止** Mermaid；**禁止**默认 `prototype-annotation`；**禁止**链接或生成 `需求评估.md` / `需求文档.md`。
4. 必须更新 `文件夹/需求/index.html` 或 `文件夹/项目/index.html` 预览链接。
5. 示例数据见 `文件夹/规范/基础数据/` 与销售主数据规范；去掉人名。

详细图示与 index 维护见 `.cursor/skills/scenario-framing-001/reference.md`。
