# 本机知识库日更说明

## 目标

每天中午 12:30 在本机自动执行：

1. 拉取 `ERP_product`、`ERP_backend`、`ERP_frontend` 最新代码。
2. 先判断本月是否需要执行“月度全量校验同步”。
3. 若月度全量校验到期且本月未成功，则优先执行月度全量校验。
4. 若本月全量校验已成功，则走日常低 token 增量同步：先生成影响清单，再按清单启动 Agent。

当前负责系统：

- 分销系统
- 销售系统
- 自营系统
- 商超系统
- GTM系统

## 执行规则

- 电脑关机：当天不执行。
- 电脑睡眠后很晚才唤醒：脚本检测到不在 `12:30-12:45` 窗口内会跳过。
- 每月 11 号开始尝试月度全量校验同步；如果 11 号电脑关机、网络失败或执行失败，第二天 12:30 会继续尝试，直到本月成功。
- 月度全量校验成功后，会写入 `本地/state/monthly-kb-sync.json`，本月剩余时间不再重复全量校验。
- 已有任务运行中：新任务跳过，避免重复写知识库。
- 日志目录：`本地/reports/daily-kb-sync/`。
- 每次拉代码后会生成影响清单：`本地/reports/daily-kb-sync/latest-impact-plan.md`。
- 每次 Agent 执行摘要会写入 `本地/reports/daily-kb-sync/*-agent-summary.md`。
- 若影响清单为空，本次不会启动 Agent，以减少 token 消耗。

## 首次配置

本地配置文件：

```bash
本地/config/daily-kb-sync.env
```

字段来源模板：

```bash
本地/config/daily-kb-sync.env.example
```

必须配置：

```bash
CURSOR_API_KEY=你的 Cursor API Key
```

可选配置：

```bash
ERP_DAILY_KB_SYSTEMS=分销系统,销售系统,自营系统,商超系统,GTM系统
ERP_DAILY_KB_MODEL=composer-2.5
ERP_DAILY_KB_NODE_VERSION=22
ERP_DAILY_KB_WINDOW_START=1230
ERP_DAILY_KB_WINDOW_END=1245
ERP_MONTHLY_FULL_SYNC_ENABLED=true
ERP_MONTHLY_FULL_SYNC_DAY=11
ERP_DAILY_KB_MAX_FILES_PER_SYSTEM=25
ERP_DAILY_KB_MAX_DIFF_LINES_PER_FILE=80
```

## 安装定时任务

```bash
./本地/scripts/install-daily-kb-sync-launchd.sh
```

安装后会创建用户级 `launchd` 任务：

```bash
~/Library/LaunchAgents/com.ckw.erp.daily-kb-sync.plist
```

## 手动试跑

脚本默认只允许在时间窗口内执行。若要临时试跑，可以先把配置里的窗口改成当前时间附近，再执行：

```bash
./本地/scripts/daily-kb-sync.sh
```

## 卸载定时任务

```bash
launchctl bootout "gui/$(id -u)" ~/Library/LaunchAgents/com.ckw.erp.daily-kb-sync.plist
rm ~/Library/LaunchAgents/com.ckw.erp.daily-kb-sync.plist
```

## 注意事项

- 该方案依赖本机在线、联网、能访问 Git 远程仓库和 Cursor API。
- 首次运行会通过本机 `nvm` 安装/启用 Node 22，并在 `本地/node_modules` 下安装 `@cursor/sdk`。
- 前后端代码只拉取，不会修改。
- 低 token 影响清单只使用 `git diff --name-status`、`--numstat` 和受限 diff 片段生成；超出上限或无法映射的变更会交给 Agent 写入待确认项。
- 月度全量校验不是整篇覆盖重写，而是全范围检查代码与知识库差异，只更新确有偏差的小节或文档。
- 知识库更新只允许写入 `ERP_product/{{系统名}}` 下的知识库、需求索引或同步日志文件。
- 知识库同步完成后，脚本会自动把各系统的知识库变更分别提交到对应分支并推送（分销→fenxiao、销售→xiaoshou、GTM→gtm、自营→ziying、产品→chanpin、商超→shangchao）。
- 推送后，如果配置了有效的 `GITHUB_TOKEN`（细粒度，需 igovee 组织授权），脚本会自动创建 PR；否则脚本会输出手动创建 PR 的链接，点击即可创建。
- 各分支只提交自己系统目录的变更，不会交叉影响，合并到 main 时不会冲突。
- 自动任务不会修改前后端代码仓库。
