---
description: 列表页顶部筛选区默认规范（宽度、可清除下拉、场景品类全选）；生成原型时须遵循，特殊场景用户会另行说明
alwaysApply: true
---

# 列表页顶部筛选区（全局 · 默认强制）

凡生成或修改 **HTML 原型、Vue/React 列表页、带顶部筛选条的看板页**，**默认**须遵守 **`文件夹/规范/列表页顶部筛选区-全局UI规范.md`**，并以 **`文件夹/规范/全局筛选与列表示范页.html`** 为唯一参考实现（类名与交互对齐该页）。

**仅当**用户或 PRD **明确说明**为特殊场景时，可偏离本规范；偏离项须在页面注释或需求中写明。

## 硬性要求（摘要）

1. **宽度**：普通下拉约 **88～120px**；SKU 模式固定 **68px**；场景品类触发器 **140～168px**；月份范围触发器约 **168～180px**（见规范 §3）。
2. **可清除**：单选 `<select>` 筛选须 `.ctl-wrap` + 透明 **×**（有值时显示，点击回「全部」并 `dispatch change`）；场景品类触发器同样须可清除（规范 §4）。
3. **场景品类**：双列浮层；右列顶部 **「全部」** 勾选（全选/全不/半选）；跨场景记忆见《销售主数据-全局业务规范》§12.2（规范 §5）。
4. **布局**：标签左控件右；项间距横 **16px**；筛选区底部分隔线；查询按钮在筛选项行末。
5. **产品维度**：SKU 组合、品线 Tag、区域↔国家、渠道↔店铺见 **`产品信息筛选区-全局UI规范.md`**；业务联动见 **`销售主数据-全局业务规范.md` §12**。
6. **专项控件**：月份 `month-range-picker.js`；日期 `date-range-picker.js`；多选懒加载 `multi-select-filter.js`；浮层防遮挡见 `dropdown-overlay-escape.mdc`。

生成前先读上述规范文档，**禁止**沿用宽下拉、无清除钮、场景品类缺「全部」的旧稿样式。
