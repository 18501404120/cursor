---
description: 销售主数据业务口径（model/SPU/SKU/MSKU/场景/品类/品线/渠道/店铺唯一键与下钻路径）
alwaysApply: false
---

# 销售主数据（全局业务）

涉及 **场景、品类、model、SPU、SKU、MSKU、品线、产品线、渠道、店铺、研发状态、产品定位** 时，必须先读 **`文件夹/规范/销售主数据-全局业务规范.md`**，并使用 **`文件夹/规范/基础数据/`** 下 JSON，禁止硬编码与基础数据不一致的枚举。

**摘要（强制）**

1. **层级**：场景 → 品类 → model（= SPU = **SKU 前 5 位**）→ SKU（8 位）→ MSKU × 店铺。
2. **唯一键**：明细行 = **`msku` + `店铺名称`**；MSKU 字符串非全局唯一。
3. **「唯一 MSKU」交互**：未选店铺时按 **MSKU 汇总各店铺**；勿按全局唯一 MSKU 取一行。
4. **品线**：4 位亚马逊品线；**品线 = 产品线**。
5. **渠道 / 店铺**：`渠道店铺基础数据_v1.json`；GoveeLife 为 **`Goveelife_US`** 等（life 小写）。
6. **术语**：文档写 **8 位 SKU / 生产 SKU**；**MSQ** 视为 **MSKU** 笔误。
7. **全渠道管报既有页面**：下钻路径规范已确立，**暂不强制改原型布局**；新需求与新页面对齐本文档。

宽表数据：`基础数据/销售基础信息_v1.json`；索引：`销售主数据索引_v1.json`。
