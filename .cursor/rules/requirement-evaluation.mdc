---
description: 当用户请求评估需求或操作 需求 文件夹时，执行需求评估并输出 需求评估.md 到对应需求文件夹
globs: 文件夹/需求/**/*
alwaysApply: false
---

# 需求评估规则

- 当用户要求「评估需求」「评估这个需求」「需求合理性」「识别不明确点」或指向 `文件夹/需求` 下某路径时，**优先使用项目 Skill：requirement-evaluation**（位于 `.cursor/skills/requirement-evaluation/SKILL.md`）。
- 读取该需求文件夹内所有可读文件及图片内容，结合 `文件夹/提示词/产品经理定位.md` 进行：
  - **合理性评估**：业务价值、与现有 ERP 模块契合度、可行性、范围与优先级、冲突与风险
  - **不明确点识别**：提出方、场景、目标与成功标准、范围边界、验收标准、时间与依赖、约束与例外
- 将评估结果写入**该需求文件夹**下的 **`需求评估.md`**，结构参照 `.cursor/skills/requirement-evaluation/evaluation-template.md`。
- 在同一文件夹下生成 **`需求文档.md`**，按 `.cursor/skills/requirement-evaluation/requirement-doc-template.md` 描述**需求需要改动的点**（涉及模块、页面/字段/筛选、逻辑、数据依赖、验收要点等），便于开发与排期。
- 若需求文件夹不存在，先创建再写入；若用户未指定文件夹名，可使用 `需求/需求-YYYYMMDD/` 并说明。
