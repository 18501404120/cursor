---
description: 批量导入功能须含模版下载、填写说明、上传区；生成或修改含导入的页面时遵循
alwaysApply: true
---

# 批量导入（全局）

凡页面含 **导入 / 批量导入 / 上传 Excel**：

1. 先读 **`文件夹/规范/批量导入-全局UI规范.md`**
2. 优先用 **`文件夹/规范/assets/import-modal-kit.js`**（`ImportModalKit.mount`）
3. **禁止**仅 `<input type="file">` 无模版与说明

**必须包含**：下载导入模版、查看模版填写要求、上传区、确认导入。

外链须可点击（见 `html-prototype-external-links.mdc`）。
