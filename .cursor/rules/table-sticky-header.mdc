---
description: 可纵向滚动的数据表格须固定表头（sticky thead），并配合滚动容器与背景/z-index
alwaysApply: true
---

# 数据表格表头固定（全局）

凡页面中存在 **可纵向滚动** 的数据表格（列表、看板明细、弹窗表格等），须遵守 **`文件夹/规范/数据表格-表头固定-全局UI规范.md`**。

**硬性要求（摘要）**

1. **滚动容器**：表格外包一层 `overflow: auto`，并设 **`max-height`**（或置于 flex 子项且 **`min-height: 0`**），使纵向滚动发生在表体区域而非随意依赖整页滚动。
2. **表头**：`thead th` 使用 **`position: sticky; top: 0`**（有固定顶栏时 `top` 为顶栏高度）；**不透明 `background`**；**`z-index: 2`**（与冻结列并存时按规范提高交叉格层级）。
3. **分隔**：建议 `box-shadow: 0 1px 0` 与边框色一致，避免滚动时表头与首行粘连难辨。
4. **表头格线**：`thead th` 的边框须在浅色背景上**清晰可辨**；多行表头时首行与次行间须有 **加粗或更深** 的水平分隔（详见规范文档「格线可辨」条）。

生成或修改 **HTML 原型、Vue/React 表格布局** 时，先读规范文档；表头格线、多行表头或冻结列按规范第 **2**（含格线可辨）、**3、4** 节处理。
