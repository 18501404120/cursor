---
description: 含产品信息（model/SKU 等）的页面筛选区须与全局 UI 规范一致
alwaysApply: true
---

# 产品信息筛选区（全局）

凡页面含**产品信息**相关筛选（model、SKU、SPU 等），顶部筛选条必须遵守 **`文件夹/规范/产品信息筛选区-全局UI规范.md`**。

**硬性要求（摘要）**

1. **布局**：单行横向；标签在左（中灰）、控件在右；垂直居中；项与项之间留出明显间距；筛选区下方一条浅灰分隔线。
2. **model 类**：`Select`，占位「请选择」，圆角、浅灰边、右侧下拉箭头。
3. **SKU**：**左侧模式下拉**（模糊 / 精确 / 8位搜索）+ **右侧输入框**（占位「请输入SKU」）组成**一体组合**；聚焦时任一子控件时**整组**用主色描边；下拉里当前项主色高亮 + 对勾。
4. **品线（4 位编码）多选**：须符合规范 **`文件夹/规范/产品信息筛选区-全局UI规范.md` §2.3**（Tag + `+N` 溢出、下拉勾选与主色 ✓、可输入过滤）；不得用原生多选 `<select multiple>` 凑合替代，除非页面明确为极简草稿。
5. **风格**：对齐 Ant Design / Element 类后台；颜色与圆角见规范文档第 4 节。

生成或修改 **HTML 原型、Vue/React 页面、需求说明中的筛选设计** 时，先读上述规范文档再动手；其它筛选项可扩展，但不得破坏该结构与视觉层级。
