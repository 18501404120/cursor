---
description: 含产品信息（model/SKU 等）的页面筛选区须与全局 UI 规范一致
alwaysApply: true
---

# 产品信息筛选区（全局）

凡页面含**产品信息**相关筛选（model、SKU、SPU 等），顶部筛选条必须遵守 **`文件夹/规范/产品信息筛选区-全局UI规范.md`**；**通用筛选项宽度、可清除下拉、场景品类「全部」** 另见 **`文件夹/规范/列表页顶部筛选区-全局UI规范.md`**（默认强制，特殊场景用户会说明）。**字段含义**见 **`文件夹/规范/销售主数据-全局业务规范.md`**。

**硬性要求（摘要）**

1. **布局**：单行横向；标签在左（中灰）、控件在右；垂直居中；项间距横 **16px**；筛选区下方一条浅灰分隔线（与《列表页顶部筛选区》一致）。
2. **model / SPU**：`Select`，占位「请选择」；**model = SPU = SKU 前 5 位**（8 位生产 SKU 的左 5 位）。
3. **SKU 组合**：**左侧模式下拉**（模糊=SKU / **精确=MSKU** / 8位=生产SKU，**宽约 68px**）+ **右侧输入框**（约 112px，占位「请输入SKU」）组成**一体组合**；与 **model** 互不级联缩选项；聚焦时任一子控件时**整组**主色描边。联动见《销售主数据-全局业务规范》§12。
4. **可清除**：区域/国家/渠道/店铺等单选下拉须有透明 **×** 清除（见《列表页顶部筛选区》§4）。
5. **场景品类**：双列浮层 + 每场景品类 **「全部」** 勾选（见《列表页顶部筛选区》§5）。
6. **品线 / 产品线（4 位，亚马逊品线）多选**：须符合规范 **`文件夹/规范/产品信息筛选区-全局UI规范.md` §2.3**（Tag + `+N` 溢出、下拉勾选与主色 ✓、可输入过滤）；**品线 = 产品线**；不得用原生多选 `<select multiple>` 凑合替代，除非页面明确为极简草稿。
7. **渠道 / 店铺**：§2.5 联动；数据源 `规范/基础数据/渠道店铺基础数据_v1.json`；GoveeLife 店铺名为 **`Goveelife_XX`**（life 小写）。
8. **风格**：对齐 Ant Design / Element 类后台；颜色与圆角见规范文档第 4 节。

生成或修改 **HTML 原型、Vue/React 页面、需求说明中的筛选设计** 时，先读上述规范文档再动手；其它筛选项可扩展，但不得破坏该结构与视觉层级。
