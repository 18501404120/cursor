---
description: 用户说「编辑原型001」「编辑原型 001」「用编辑原型001」「页内编辑原型」「prototype-edit-001」或同义表达，要求对 HTML 原型注入页内编辑能力、导出 sidecar、或 Agent 修改原型时保留页内编辑结果时，必须读取并严格按 `.cursor/skills/prototype-edit-001/SKILL.md` 执行。
alwaysApply: false
---

# 编辑原型001（phrase 触发）

## 必须执行

1. **先读取** Skill 全文：`.cursor/skills/prototype-edit-001/SKILL.md`（含运行时、sidecar、AI 持久化策略）。
2. **目标页面**：用户说「当前原型」时，优先取**当前聚焦或最近编辑的 HTML**；不明确则**只问一次**路径。
3. **注入/升级运行时**：`python3 .cursor/skills/prototype-edit-001/scripts/inject_prototype_edit.py --html <path>`（默认 `--preserve-edits`）。
4. **Agent 后续改 HTML**：**必须先读** `{basename}.prototype-edit.json`；遵守 [ai-persistence-policy.md](../skills/prototype-edit-001/references/ai-persistence-policy.md) — **不得**回滚 hidden/text/mock/added 除非用户明确撤销。

## 与原型标注001

- 可同时存在：`prototype-edit` 左上角工具条；`prototype-annotation` 右上角说明。
- 重生成标注 **不得** 删除 `prototype-edit` 块或 sidecar。

## 不要做的事

- 不得在未读 sidecar 情况下「恢复」被页内隐藏的筛选项/列/按钮。
- 不得删除用户导出的 `{page}.prototype-edit.json`（除非用户明确要求清除页内编辑）。
