---
description: 弹层/对话框标题栏的图标关闭（×）须位于右上角；与红点并存时用标题区+右侧操作组避免 × 居中。
globs:
  - "文件夹/项目/**/*.html"
  - "文件夹/需求/**/需求原型.html"
alwaysApply: false
---

# 弹层关闭按钮（右上角）

生成或修改 **HTML 原型 / 说明版原型** 中的 Modal、Dialog、Drawer 时：

1. **图标关闭（×）** 必须放在 **标题栏最右侧**（视觉右上角），详见 **`文件夹/规范/弹层与对话框-关闭按钮-全局UI规范.md`**。
2. 标题栏使用 **flex** 且需在关闭钮旁加 **红点（`proto-red-dot`）** 等节点时：将 **× 与红点** 包在同一 **右侧容器**（如 `*-hd-actions` / `mh-close-group`）内；**标题**单独占 `flex:1` 区域，**禁止**「标题、×、红点」三者作为三个平级 flex 子项 + `space-between` 导致 × 落到水平中间。
3. 需求原型生成（`requirement-prototype`）与 `prototype-anno-kit` 内建说明弹层须遵守同一结构（参见规范文档中的 kit 示例）。
