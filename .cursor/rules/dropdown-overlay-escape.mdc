---
description: 自定义下拉/浮层面板须防父级 overflow 裁切与板块遮挡；默认 body 挂载 + fixed
alwaysApply: true
---

# 下拉与浮层防遮挡（全局）

凡 **自绘** 的下拉、日期/月份面板、多选筛选层等（非原生 `<select>` 系统菜单），须遵守 **`文件夹/规范/下拉与浮层-防遮挡-全局UI规范.md`**。

**摘要**

- **禁止**依赖「仅提高 `z-index`」解决被裁切：若祖先有 **`overflow: hidden/auto`**（含表格 **`overflow-x: auto`** 带来的裁切），面板仍会被裁掉。  
- **默认策略**：展开时挂 **`document.body` + `position: fixed`**，按触发器 **`getBoundingClientRect()`** 定位；关闭时挂回占位节点。  
- **本仓库**：`month-range-picker.js`、`multi-select-filter.js` 已默认 **`useBodyPortal: true`**；特殊布局可显式 `useBodyPortal: false`（仅当确认无裁切祖先时）。

生成或修改 HTML 原型、封装新浮层控件时，先读上述规范文档；Ant Design 等用 **`getPopupContainer`** 对齐同一思路。
