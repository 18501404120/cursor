---
description: HTML 原型与说明页中，面向用户的外链须为可点击超链接（禁止仅裸 URL）；详见规范文档
alwaysApply: false
---

# HTML 原型 · 内嵌外链（可点击）

编写或修改 **`文件夹/项目/`**、`docs/` 下 **HTML 原型 / 说明页** 时，凡在页面、红点气泡、`innerHTML`、弹层正文中出现的 **http(s) URL**，须遵守 **`文件夹/规范/HTML原型-内嵌URL超链接-全局UI规范.md`**。

**摘要**：使用 `<a href="完整URL" target="_blank" rel="noopener noreferrer">语义化文案</a>`；红点浮层内点击不抢先关闭；勿在 `alert` 中作为唯一可点链入口。
