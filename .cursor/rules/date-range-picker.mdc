---
description: 日期范围筛选/表单须使用双月历日期范围选择规范与共用脚本
alwaysApply: true
---

# 日期范围选择框（全局）

凡页面出现 **起止日期**、**时间范围（按日）** 筛选或录入，须遵守 **`文件夹/规范/日期范围选择框-全局UI规范.md`**，并优先使用 **`文件夹/规范/assets/date-range-picker.js`**（`DateRangePicker.mount`）生成与图示一致的交互。

**摘要**

- 展示：`YYYY-MM-DD 至 YYYY-MM-DD`，左侧日历图标，可选清空。  
- 交互：点击展开 **双月历** 面板，范围高亮（主色端点 + 浅蓝中间）。  
- 防遮挡：`mount` 默认 **`useBodyPortal: true`**（面板挂 `document.body` + `fixed`）；与《下拉与浮层-防遮挡》一致，仅在确认无 `overflow` 裁切祖先时可显式 `false`。  
- 禁止：用两个原生 `<input type="date">` 拼「起～止」作为最终形态（原型交付须为上述一体化控件）。  
- 单月、单日需求不在此规则范围（见规范文档第 4 节）。

生成或修改 **HTML 原型、说明文档中的控件示例** 时，先读规范文档再实现。
