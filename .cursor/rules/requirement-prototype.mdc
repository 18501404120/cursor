---
description: 根据需求文档生成可预览、可交互的 HTML 原型并保存到需求文件夹。当用户要求生成需求原型、前端原型、可预览原型时，使用 requirement-prototype Skill。
globs: 文件夹/需求/**/需求文档.md
alwaysApply: false
---

# 需求原型规则

- 当用户要求「根据需求文档生成原型」「生成需求原型」「用前端展示原型」「支持预览和交互」或指向 `文件夹/需求` 下某文件夹时，**优先使用项目 Skill：requirement-prototype**（`.cursor/skills/requirement-prototype/SKILL.md`）。
- 读取该需求文件夹下的 **`需求文档.md`**，按 `.cursor/skills/requirement-prototype/prototype-guide.md` 生成**单文件 HTML**，包含筛选区、表格/内容区、模拟数据与基础交互（日期、下拉、Tab、排序、查询）。
- 将生成的文件保存为**该需求文件夹**下的 **`需求原型.html`**；用户用浏览器直接打开即可预览与操作。
- 若该文件夹下无 `需求文档.md`，先提示需先生成需求文档（运行需求评估 Skill），或根据现有需求内容/评估文档提炼改动点后再生成原型。
