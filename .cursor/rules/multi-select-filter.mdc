---
description: 多选下拉筛选（懒加载、搜索、默认「全部」）全局规范
alwaysApply: false
---

# 多选下拉筛选（全局）

凡页面筛选项为 **「枚举多选」**（零售商、场景、品类、MSKU 列表等），须遵守 **`文件夹/规范/多选下拉筛选-全局UI规范.md`**，并优先使用 **`文件夹/规范/assets/multi-select-filter.js`**（`MultiSelectFilter.mount`）。

**摘要**

- **懒加载**：默认不渲染选项；**首次点击展开**后再执行 `getOptions`（或请求接口）。
- **面板**：顶部 **搜索** + 默认 **首行「全部」**（勾选当前搜索可见项）+ **多选勾选**。
- **例外**：**SKU / MSKU** 等大数据量场景可 **`showSelectAll: false`**，须在页面或 PRD 单独说明；未声明例外则必须有「全部」。

生成或修改 HTML 原型、Vue/React 筛选条时，先读规范文档再动手。

**防遮挡**：`multi-select-filter.js` 默认 `useBodyPortal: true`（见 **`文件夹/规范/下拉与浮层-防遮挡-全局UI规范.md`** 与 `dropdown-overlay-escape` 规则）。
