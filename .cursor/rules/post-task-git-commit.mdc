---
description: 任务收尾强制本地 git commit；默认不 push；用户明确要求时再推送
alwaysApply: true
---

# 任务完成后 Git 提交（强制）

## 强制要求（MUST）

1. 在本仓库内**只要对用户交办内容做了实质性修改**（新建/改写/删除文件），在**同一轮对话、任务收尾前**必须完成本地提交，**禁止**只改文件不提交、也**禁止**把提交留给用户口头再催。
2. 执行顺序：**先** `git status` 确认改动 → **再** 仅 `git add` 与本次任务相关的路径 → **`git commit`**。若 `git status` 已干净，可跳过 git。
3. **默认不执行 `git push`**。仅当用户在本轮对话中**明确**要求推送/同步远程（含「push」「推送」「传到 GitHub」等）时，再在 commit 成功后尝试 `git push`；push 失败（认证、网络、无 upstream）则说明原因并保留本地 commit 即可。

## 暂存范围

- **只 add 本次任务实际动过的文件**（或同一需求下的紧邻文件）；禁止使用 `git add -A` / `git add .` 把无关未跟踪文件、`.DS_Store`、编辑器垃圾一并纳入，除非用户明确要求「全量提交」。
- **禁止提交**：`.env`、`*.pem`、密钥、含 token 的本地配置；用户声明「不要提交」「仅本地」的内容。
- 若误改了无关文件，应先还原或排除，再提交，避免污染历史。

## 提交信息

- 一条清晰说明，中文或英文均可；建议 Conventional Commits 风格：  
  `type(scope): 简述`  
  例如：`docs(销量预测): 配置中心需求 v1.3 与 4.2 场景降级`、`feat(管报): 项目规划原型调整筛选区`。
- 多文件同一主题可合并为一次 commit；**不要**为每个文件机械拆成多条无意义 commit。

## 失败与边界

- **`git commit` 失败**（未配置 `user.name`/`user.email`、不在 git 仓库、hook 拒绝等）：在回复中**简要说明原因**和**用户需执行的一条命令或设置**，不要静默跳过。
- **仓库尚无首笔 commit**：在合法前提下可正常创建首 commit；若用户意图是「不要动 git 历史」，以其当轮说明为准。
- **纯只读任务**（仅回答问题、未改任何文件）：无需 commit。

## 与用户手动操作的关系

用户可随时自行 `commit` / `amend` / `push`；本规则只约束**助手**在交付修改时的收尾行为，避免「每次都要用户提醒才提交」。
