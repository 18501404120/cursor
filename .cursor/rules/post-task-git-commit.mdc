---
description: 任务收尾强制 commit；仅 文件夹/项目 或 文件夹/需求 下改动自动 push；其余不 push
alwaysApply: true
---

# 任务完成后 Git 提交（强制）

## 强制要求（MUST）

1. 在本仓库内**只要对用户交办内容做了实质性修改**（新建/改写/删除文件），在**同一轮对话、任务收尾前**必须完成本地提交，**禁止**只改文件不提交、也**禁止**把提交留给用户口头再催。
2. 执行顺序：**先** `git status` 确认改动 → **再** 仅 `git add` 与本次任务相关的路径 → **`git commit`**。若 `git status` 已干净，可跳过 git。
3. **`git push` 策略（按优先级）**  
   - **A. 自动 push**：若本次 `git add` 纳入的**任意文件路径**落在仓库根下 **`方案设计/文件夹/项目/`** 或 **`方案设计/文件夹/需求/`** 目录树内（含子目录），则在 **`git commit` 成功后必须尝试 `git push`**，推送**当前所在分支**到 `origin`（与日常协作一致，**不限制分支名**）。若当前分支尚无 upstream，使用 `git push -u origin HEAD`（或等价方式）建立跟踪后再推；若仍失败（认证、443、权限），在回复中说明原因并保留本地 commit。  
   - **B. 默认不 push**：若本次提交**不涉及** `方案设计/文件夹/项目/` **且**不涉及 `方案设计/文件夹/需求/` 下任何文件，则**不**主动 `git push`，除非用户在本轮对话中**明确**要求推送（含「push」「推送」「传到 GitHub」等）。  
   - **C. 混合改动**：同一 commit 中**既有** `方案设计/文件夹/项目/` 或 `方案设计/文件夹/需求/` **又有**其他路径时，仍按 **A** 执行（commit 后尝试 push 整次提交）。

## 自动 push 所指的物理路径

- 以 Git 仓库根（本仓库即含 `.git` 的 **`本地/`** 目录）为基准，前缀匹配（二选一即触发 A）：  
  - `方案设计/文件夹/项目/`  
  - `方案设计/文件夹/需求/`  
- 等价于磁盘上的：`…/本地/方案设计/文件夹/项目/`、`…/本地/方案设计/文件夹/需求/`。

本仓库 **`.githooks/post-commit`**（位于 `本地/.githooks/`）与上述策略一致：仅当本次提交包含上述前缀之一时自动 `git push`；其余路径的提交不会自动推送。

## 暂存范围

- **只 add 本次任务实际动过的文件**（或同一需求下的紧邻文件）；禁止使用 `git add -A` / `git add .` 把无关未跟踪文件、`.DS_Store`、编辑器垃圾一并纳入，除非用户明确要求「全量提交」。
- **禁止提交**：`.env`、`*.pem`、密钥、含 token 的本地配置；用户声明「不要提交」「仅本地」的内容。
- 若误改了无关文件，应先还原或排除，再提交，避免污染历史。

## 提交信息

- 一条清晰说明，中文或英文均可；建议 Conventional Commits 风格：  
  `type(scope): 简述`  
  例如：`docs(销量预测): 配置中心需求 v1.3 与 4.2 场景降级`、`feat(管报): 项目规划原型调整筛选区`。
- 多文件同一主题可合并为一次 commit；**不要**为每个文件机械拆成多条无意义 commit。

## 失败与边界

- **`git commit` 失败**（未配置 `user.name`/`user.email`、不在 git 仓库、hook 拒绝等）：在回复中**简要说明原因**和**用户需执行的一条命令或设置**，不要静默跳过。
- **仓库尚无首笔 commit**：在合法前提下可正常创建首 commit；若用户意图是「不要动 git 历史」，以其当轮说明为准。
- **纯只读任务**（仅回答问题、未改任何文件）：无需 commit。

## 与用户手动操作的关系

用户可随时自行 `commit` / `amend` / `push`；本规则只约束**助手**在交付修改时的收尾行为，避免「每次都要用户提醒才提交」。
