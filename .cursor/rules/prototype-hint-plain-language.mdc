---
description: HTML 原型中红点（编号说明）与蓝 i（列说明）文案须业务可读，避免开发腔与无上下文字段名
globs:
  - "文件夹/项目/**/*.html"
  - "文件夹/**/原型/**/*.html"
alwaysApply: false
---

# 原型红点与「i」提示文案（全局）

适用于含 **红色编号按钮**、**蓝色 i 列说明**、筛选/表格旁说明条、以及弹层内引导语的 **HTML 原型**（含 ERP 管报类页面）。

## 必须做到（MUST）

1. **读者**：产品、业务、测试、研发都应能读懂，**不依赖**读源码或猜英文字段含义。
2. **禁止**在面向人的文案中出现（含弹窗正文、Toast、`aria-describedby` 式长说明、页内灰色提示条）：
   - 未加解释的内部字段名、接口名、存储键（如 `sceneCategoryLeaves`、`localStorage`、裸 `embed` 脚本 id 等）；
   - 无中文解释的英文技术词（如 `eligible`、裸写 `OR`/`AND`）；
   - 「[开发范围]」类 **无上下文** 的标签句；
   - 把实现细节当用户提示（除非明确是「仅研发可见」的单独文档链接，且仍应用业务语言概括）。
3. **必须**写清 **谁、在什么条件下、得到什么结果**；关键规则用 **短句分条**，避免一段话堆叠多个概念。
4. **红点（编号）**与 **蓝 i（列说明）** 分工：
   - 蓝 i：本列/本筛选项 **是什么、怎么填、与别列关系**（偏业务操作）；
   - 红点：本条规则 **补充边界、保存校验、拆分口径**（仍用业务语言，可略细，但不得堆字段名）。

## 建议（SHOULD）

- 数学关系用中文：「重叠」「同时满足」「且 / 或」，少用符号堆砌。
- 与全站控件规范一致时，可写「与全站月份范围控件样式一致」，避免甩文档文件名代替解释。
- 原型落库写「记在浏览器本机（演示用）」等，不写具体 API。

## 落地检查

改完原型后全文检索：`localStorage`、`eligible`、反引号包字段、仅英文缩写无中文 —— 应无命中（**代码脚本内变量名除外**，但 **不得** 出现在用户可见字符串中）。
