---
description: 按自然月的起止筛选须使用月份范围选择规范与 month-range-picker，禁止用按日日期范围冒充「月份范围」
alwaysApply: true
---

# 月份范围选择框（全局）

凡标签或业务含义为 **「月份范围」「起止月」「YYYYMM」** 的筛选/表单，须遵守 **`文件夹/规范/月份范围选择框-全局UI规范.md`**，并优先使用 **`文件夹/规范/assets/month-range-picker.js`**（`MonthRangePicker.mount`）。

**摘要**

- 展示：**`YYYY-MM - YYYY-MM`**（中间 `-` 两侧空格），日历图标 + 清空；**禁止**在「月份范围」上使用 `YYYY-MM-DD 至 YYYY-MM-DD`。  
- 面板：**连续两年** 的 **3×4 月格**，`«` / `»` 切换年份对。  
- 与《日期范围》规范 **视觉对齐**，**粒度不同**（月 vs 日）。

生成或修改 HTML 原型、需求说明中的控件时，先读规范文档；需要按日时再使用 `date-range-picker.js`。
