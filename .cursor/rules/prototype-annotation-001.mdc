---
description: 用户说「原型标注001」「原型标注 001」「通过原型标注001对当前原型页面进行标注」「用原型标注001」「原型标注1」或同义表达，要求对 HTML/Web 原型做业务标注、页面说明、刷新标注时，必须读取并严格按 `.cursor/skills/prototype-annotation/SKILL.md` 执行。
alwaysApply: false
---

# 原型标注001（phrase 触发）

## 必须执行

1. **先读取** Skill 全文：`.cursor/skills/prototype-annotation/SKILL.md`（含资源路径、运行时、注入脚本与清单）。
2. **目标页面**：用户说「当前原型页面」时，优先取**当前聚焦或最近编辑的 HTML**；若仍不明确，**只问一次**让用户给出文件路径。
3. **按 Skill 工作流**完成：巡检页面 → 只标高价值控件 → 生成/替换唯一配置块与运行时 → 校验 **`.pa-toolbar`（查看页面说明 + 添加标注）** 与 version 策略。
4. **运行时**：须使用 Skill 内最新 `prototype-annotation-runtime.js` / `.css`（含 **添加标注** 点选模式；Git Pages **假权限**只读见 Skill §Git Pages 编辑权限 与 `references/edit-policy-setup.md`）。

## 本仓库资源路径（与 Skill 内 Codex 路径区分）

- Skill、脚本与样式以本仓库为准：`.cursor/skills/prototype-annotation/` 下 `assets/`、`scripts/`（若 Skill 正文出现其他磁盘绝对路径，以本仓库同名文件为准）。

## 不要做的事

- 不要与旧版标注配置**合并**；每次执行按 Skill「整页重生成、升 version」。
- 不要在标注文案中暴露代码名、接口名、表名等实现细节（见 Skill「Content Standards」）。
